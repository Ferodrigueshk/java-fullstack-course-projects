
public class aula002 {

    public static void main(String[] args) {
        /*
 * A compilação é o processo de tradução de um código-fonte, escrito por um programador
 * em uma linguagem de alto nível, para um código objeto, que é compreendido pela máquina.
 * Esse processo ocorre antes da execução do programa e gera um arquivo binário independente,
 * como os arquivos .exe no Windows. Linguagens como C e C++ utilizam esse modelo.
 *
 * Já a interpretação não gera um arquivo binário final. Em vez disso, um interpretador lê
 * e executa o código linha por linha, durante o tempo de execução. Isso permite maior
 * flexibilidade e facilita testes rápidos, mas pode ser mais lento. Exemplos de linguagens
 * interpretadas incluem Python e JavaScript.
 *
 * O código-fonte é o texto original escrito pelo programador, contendo as instruções em uma
 * linguagem compreensível por humanos, como Java, C ou Python. Esse código precisa ser
 * transformado para que o computador o entenda.
 *
 * O código objeto é o resultado da compilação do código-fonte. Ele é uma versão intermediária
 * em linguagem de máquina, mas ainda não é executável sozinho — normalmente, precisa passar
 * por um processo de linkagem para formar um programa completo.
 *
 * A máquina virtual, como a JVM (Java Virtual Machine), oferece uma forma híbrida entre
 * compilação e interpretação. O código-fonte é primeiro compilado para um formato intermediário
 * (bytecode), e esse bytecode é interpretado ou compilado dinamicamente pela máquina virtual,
 * permitindo portabilidade entre diferentes sistemas operacionais.
         */

    }
}
